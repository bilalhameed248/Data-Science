doc = fitz.open("Resume_Template_by_Anubhav2.pdf")    
l = list(range(doc.pageCount))

print(doc.get_page_text(1).strip())
print(doc.get_page_text(2).strip())
print(doc.get_page_text(3).strip())

for i in l:
    if doc.get_page_text(i).strip() == '' or doc.get_page_text(i).strip() == '◦' or doc.get_page_text(i).strip() == '' :
        l.remove(i)                     
doc.select(l)                           
doc.save("some-non-empty.pdf", garbage=3)
doc.close()
